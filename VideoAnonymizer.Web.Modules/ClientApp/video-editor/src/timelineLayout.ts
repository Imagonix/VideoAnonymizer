/**
 * Shared expanded-timeline layout contract.
 * Label column spacers/rows and occurrence column headers/rows must use these
 * exact values so each label lines up with its occurrence row.
 *
 * Keep in sync with CSS custom properties on `.timeline-wrapper` in VideoEditorApp.vue.
 */
export const TIMELINE_TOOLBAR_HEIGHT_PX = 36;
export const TIMELINE_OVERVIEW_HEIGHT_PX = 28;
export const TIMELINE_OVERVIEW_GAP_PX = 4;
export const TIMELINE_RULER_HEIGHT_PX = 22;
export const TIMELINE_RULER_GAP_PX = 6;
export const TIMELINE_ROW_HEIGHT_PX = 28;
